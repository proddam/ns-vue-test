// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "com.prosigner.vuetest"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
